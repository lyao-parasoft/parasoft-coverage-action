name: "Luffy Build"

on:
  workflow_dispatch:  # 启用手动触发

jobs:
  build:
    runs-on: luffy-windows
    steps:
      - uses: actions/checkout@v4
      - name: Generate table
        uses: parasoft/parasoft-coverage-action@master
        with:             
            report: 'D:\reports\parasoftreports\static_unit_coverage_reports\reports-linux\Cpp-Pro\Flowanalysis\2024.1.0\coverage\coverage.xml'
