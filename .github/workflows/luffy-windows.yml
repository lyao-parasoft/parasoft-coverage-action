name: "Luffy Build"

on:
  workflow_dispatch:  # 启用手动触发

jobs:
  build:
    runs-on: [self-hosted,luffy-windows]
    steps:
      - uses: actions/checkout@v4
      - name: Generate table
        uses: parasoft/parasoft-coverage-action@master
        with:             
            report: 'D:\reports\large-reports\JavaProjectTemplate-perfomanceTest-1000P-88006V-62002T-724014L-60C\report\jtest\junit\report'
            parasoftToolOrJavaRootPath: 'C:\Program Files\Java\jdk&11'
