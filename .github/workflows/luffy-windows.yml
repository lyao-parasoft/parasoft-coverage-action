name: "Luffy Build"

on:
  workflow_dispatch:  # 启用手动触发

jobs:
  build:
    runs-on: [self-hosted,luffy-windows]
    steps:
      - uses: actions/checkout@v4
      - name: Generate table
        uses: parasoft/parasoft-coverage-action@master
        with:             
            report: 'D:\reports\large-reports\JavaProjectTemplate-perfomanceTest-2000P-124002T-1448014L-60C\report\jtest\junit\coverage.xml'
